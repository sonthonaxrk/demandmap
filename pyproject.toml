[build-system]
requires = ["maturin>=1.7,<2"]
build-backend = "maturin"

[project]
name = "demandmap"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = ["numpy"]

[tool.maturin]
features = ["extension-module"]
